function timeToAlert() {
    alert("The time is 8:30 AM");
}
var timeIsBeing830 = new Date("11/13/2021 08:30:00 AM").getTime(
, currentTime = new Date ().getTime()
, subtractMilliSecondsValue =  timeIsBeing830 - currentTime;
setTimeout(timeToAlert, subtractMilliSecondsValue);

npm Node.$(selector).scroll(function () { 
// node email selector & node emailer creation integrate node.js into SQL tables clock with time trigger.
var nodemailer = 
require ('nodemailer');

var transporter = 
nodemailer.createTransport({
    service: 'gmail',
    auth: {
        user: 'youremail@gmail.com',
        pass: 'yourpassword'
    }
});

var mailOptions = (mailOptions, function(error, info){
    if (error) {
        console.log(error);
    } else {
        console.log(error);
    } else {
        console.log('Email sent: ' + info.response);
}
});